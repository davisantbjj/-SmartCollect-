{
  "name": "smartcollect",
  "private": true,
  "version": "1.0.0",
  "description": "SmartCollect — Plataforma de cobrança multi-tenant (monorepo)",
  "scripts": {
    "dev:all": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/dev-all.ps1",
    "stop:all": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/stop-all.ps1",
    "build:frontend": "npm run build --prefix frontend",
    "lint:frontend": "npm run lint --prefix frontend"
  }
}
